# Copy to .env for local reference. Never commit real secrets.
PORT=8080
APP_ENV=development
GREETING_PREFIX=Hello
# Optional: serve static files from a directory on disk instead of the classpath
# STATIC_FILES_PATH=C:/path/to/webroot
